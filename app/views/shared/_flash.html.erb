<% flash.each do |type, message| %>
  <% css_class = type.to_s == "alert" ? "bg-red-50 text-red-700 border-red-200" : "bg-green-50 text-green-700 border-green-200" %>
  <div class="mb-4 rounded-lg border px-3 py-2 text-sm <%= css_class %>"><%= message %></div>
<% end %>
