Hello <%= @user.name %>,

We received a request to reset your password.

Reset link (valid for 30 minutes):
<%= url_for(controller: "passwords", action: "edit", token: @token, only_path: false) %>

If you did not request this, you can ignore this email.
