<section class="space-y-4">
  <h1 class="text-3xl font-semibold tracking-tight text-[var(--color-ink)]">Easy Vote</h1>
  <p class="max-w-xl text-[var(--color-muted)]">
    You are signed in as <span class="font-medium text-[var(--color-ink)]"><%= current_user.name %></span>.
    Next step is building feature submission and voting flows.
  </p>
</section>
